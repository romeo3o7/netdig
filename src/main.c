int main() {
		// make sure the interface is up [done]
		// ip exists [done] 
		// can ping out local devices (possibly by trying to ping itself)
		// can ping gateway
		// can ping outdoors (using udp)
		// can resolve domain names
		return 0;
}
